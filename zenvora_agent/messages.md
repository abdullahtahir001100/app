# Zenvora Agent Messages

Agent-facing codes live in `src/messages.rs`.

**Canonical catalog (all sides):** [`../MESSAGES.md`](../MESSAGES.md) and [`../shared/zenvora-messages.json`](../shared/zenvora-messages.json).

Keep Rust constants aligned with that JSON (same code numbers + text).
